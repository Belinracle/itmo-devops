phantom:
  address: example.com
  port: 80
  ssl: false
  load_profile:
    load_type: rps
    schedule: step(10, 100, 10, 1m)  # Каждую минуту +10 RPS до 100
  instances: 1000  # максимальное количество параллельных соединений
  ammo_file: ammo.txt  # файл с запросами

console:
  enabled: true